# HiKO 侧边导航 · 集成说明

## 结论：设计可用，但必须改变接入方式

AI 给出的 `HiKO Sidebar Demo.html` 视觉设计可用（色板确实取自首页），
但它的 **DOM 结构不能直接用于生产**，原因见下。

## 原设计的问题

| 问题 | 原设计做法 | 后果 |
|---|---|---|
| 固定舞台 | `.stage{width:1920px;height:1080px}` + `transform:scale()` | 首页响应式失效、video/canvas 模糊 |
| 首页是截图 | `background-image:url('dashboard-screenshot.png')` | 只是静态图，非真实页面 |
| 全局样式 | `*{margin:0;padding:0}`、`html,body{overflow:hidden}` | 污染首页样式 |
| CSS 变量重定义 | 重新定义 `--panel` 等 | 与首页变量冲突 |

原设计的 README 自己也指明生产环境应改用 iframe 外壳方案。

## 本次采用的方案：iframe 外壳

```
/v2  →  templates/shell.html
        ├── <iframe src="/max">     ← 首页原样加载，不缩放、不注入样式
        ├── 左侧 96px 导航栏          ← 新增
        └── 5 个评估弹窗              ← 新增
```

### 为什么首页零影响

1. **iframe 是浏览器级隔离边界** —— 外壳的 CSS/JS 无法进入 iframe，
   首页的 CSS/JS 也无法泄漏出来。
2. **不缩放** —— iframe 宽高为 100%，首页在其中保持原生响应式行为
   （`min(1520px, 100vw-20px)` 与三档媒体查询照常生效）。
3. **不改任何现有文件** —— `templates/index.html` 与
   `/`、`/jc`、`/max` 三个路由一个字节都不动。

### 几何对齐（与首页实测值一致）

| 参数 | 值 | 来源 |
|---|---|---|
| 顶栏高度 | 70px | 首页 `.bg-top{height:70px}` |
| 导航栏宽度 | 96px | 设计稿 |
| 首页主容器 | `min(1520px, 100vw-20px)` | 首页 `.con-box` |

弹窗与遮罩定位为 `top:70px; left:96px; right:0; bottom:0`，
因此首页顶栏（含右侧状态徽标）始终完整可见。

### 双模式自适应（避免导航栏遮挡内容）

- **叠加模式**（视口 ≥ 1712px）：导航栏浮在首页左侧留白上，首页完全不受影响。
  1712 = 1520(首页) + 96×2(左右留白)
- **推挤模式**（视口 < 1712px）：iframe 左移 96px 并相应缩窄，
  首页在剩余空间内自行重排。

实测各视口下首页内容左缘均 ≥ 96px，**任何宽度都不会被遮挡**。

## 文件清单

| 文件 | 说明 |
|---|---|
| `templates/shell.html` | 外壳页：导航栏 + 弹窗 + 交互（新增） |
| `static/shell_panels.js` | 5 个评估面板内容（新增） |
| `deploy_shell.sh` | 部署脚本，含备份/校验/回滚 |

## 部署

```bash
# 1. 预检（不改动任何文件）
bash deploy_shell.sh --dry-run

# 2. 安装
bash deploy_shell.sh

# 3. 重启服务
bash /home/lsz/real_time_plus/real_time_Demo/start_gunicorn_8801.sh

# 4. 访问
#    新页面： https://<地址>:8801/v2
#    原首页： https://<地址>:8801/max  （不受影响）

# 出问题回滚
bash deploy_shell.sh --rollback
```

部署脚本内置的安全措施：
- 修改 `test2.py` 前自动备份
- 自动保留原文件换行风格（LF/CRLF）
- 写入后做 `py_compile` 语法检查，失败**自动回滚**
- 用 md5 校验 `templates/index.html` 前后一致，确认首页零改动
- 校验 `/`、`/jc`、`/max` 三路由仍存在
- 幂等，可重复执行

## 待办：接入真实数据

当前弹窗内数值**全部为示例数据**（弹窗头部已标注「示例数据 · 未接入实时模型」）。
接入时替换 `static/shell_panels.js` 各 `render()` 中的硬编码值即可，
可复用首页已有接口：

| 接口 | 用途 |
|---|---|
| `/get_hr` | 心率 |
| `/get_wave_data` | rPPG 波形 |
| `/get_openface` | AU / 视线 / 表情 |

心理量表（PSS/GAD-7/PHQ-9）、肤质八维、场景评估等目前后端尚无对应算法，
需要先确定计算口径。

## 上线记录

### 2026-08-12 15:32 首次部署（8801 生产实例）

执行 `bash deploy_shell.sh` 完成安装，随后 `bash start_gunicorn_8801.sh` 重启服务。

安装校验结果：

| 校验项 | 结果 |
|---|---|
| `templates/index.html` md5 | `a6f582c049f1a5e86662d36e2184983d` 前后一致 |
| `test2.py` diff | `2048a2049,2068` — **纯追加，0 行被修改** |
| `py_compile` 语法检查 | 通过 |
| 备份文件 | `test2.py.bak_shell_2026-08-12_153217` |

路由连通性（重启后 `curl -sk https://127.0.0.1:8801/...`）：

| 路由 | 状态 | 响应大小 |
|---|---|---|
| `/v2` | 200 | 19782 |
| `/max` | 200 | 108193 |
| `/` | 200 | 21052 |
| `/jc` | 200 | 20954 |
| `/static/shell_panels.js` | 200 | 29254 |

访问地址：

- 新页面 `https://39.183.171.185:8801/v2`
- 原首页 `https://39.183.171.185:8801/max`（保持原样，未受影响）

注意事项：

- 重启 gunicorn 会清空内存态会话（`rppg_app.sessions`），重启前已打开的页面会出现一批 `400`，**刷新页面即恢复**，属预期行为。
- 自签证书，浏览器首次访问需点「高级 → 继续前往」。
- 回滚：`bash deploy_shell.sh --rollback`，恢复 `test2.py` 备份，`/v2` 消失，其余不变。

### 2026-08-12 15:51 新界面提升为默认入口

执行 `bash promote_shell.sh`，把 `/max` 切换为新界面，便于后期统一操作。

路由映射变化：

| 路由 | 切换前 | 切换后 | 说明 |
|---|---|---|---|
| `/max` | index.html | **shell.html** | 默认入口，新界面 |
| `/max_inner` | 不存在 | **index.html** | 新增，供外壳 iframe 内层加载 |
| `/v2` | shell.html | shell.html | 历史别名，老链接继续可用 |
| `/`、`/jc` | 不变 | 不变 | 未触碰 |

#### 必须新增 `/max_inner` 的原因

`shell.html` 的 iframe 原本 `src="/max"`。若 `/max` 改为渲染 `shell.html`
而 iframe 仍指向 `/max`，外壳会在自己的 iframe 中**递归加载自身**，
浏览器直接卡死。因此内层必须拆成独立路由，`shell.html` 的 iframe
同步改为 `src="/max_inner"`。

`promote_shell.sh` 内置递归自检：若线上 `shell.html` 的 iframe 仍指向
`/max`，直接拒绝并回滚。

#### 改动范围

`test2.py` diff 仅 **2 行修改 + 1 段新增**：

```
2036c2036, 2039c2039   render_template('index.html') -> ('shell.html')
2052a2053,2074          新增 /max_inner 路由
```

`templates/index.html` 仍为 `a6f582c049f1a5e86662d36e2184983d`，**一字节未改**。
`/max` 路由的函数名保持 `indexNew` 不变，避免影响任何 `url_for` 引用。

#### 验证结果

| 路由 | 状态 | 大小 |
|---|---|---|
| `/max` | 200 | 20349（外壳页）|
| `/max_inner` | 200 | 108193（原首页）|
| `/v2` | 200 | 20349 |
| `/` | 200 | 21052 |
| `/jc` | 200 | 20954 |

**关键证据**：`/max_inner` 的渲染结果 md5 = `5d6e763d324e337a94e57f287f7f5b4e`，
与切换前 `/max` 的渲染结果**完全一致** —— 原首页输出逐字节未变。

递归自检：`/max` 返回的 HTML 中 iframe 指向 `/max_inner`；`/max_inner`
内部 `<iframe>` 数量为 **0**，链路终止，结构上不可能循环。

#### 还原

```bash
bash promote_shell.sh --revert
```

从 `test2.py.bak_promote_2026-08-12_155106` 恢复，`/max` 回到原首页，
同时把 `shell.html` 的 iframe src 回退为 `/max`。

---

## 2026-08-12 17:16 · 心理综合评估改造为 v3 新版（方案 A）

### 需求
> 「我重构了心理综合评估页面，现在开始第一个工程，把首页的心理综合评估页面的前端显示改造成新版的」
> 方案选择：**方案 A**

### 设计稿与外壳的结构冲突
设计稿《心理综合评估 v3.html》是一个**完整独立页面**，自带：

| 设计稿自带 | 外壳已有 | 冲突 |
|---|---|---|
| 顶栏 56px | `.p-head` 58px | 双顶栏 |
| 左导航 76px | `.sidenav` 96px | 双导航 |
| 1920×1080 固定画布 + `transform:scale()` | 弹窗流式布局 | 文字/图表被缩放模糊 |

**方案 A 处理方式**：剥离设计稿的顶栏与左导航，**只取主内容区**嵌入外壳弹窗 `.p-body`；
固定画布缩放改为**响应式流式布局**（依据设计稿 README「Implementation Notes 第 1 条」）。

### 变更文件（3 个，全部前端资源，后端零改动）

| 文件 | 操作 | 大小变化 |
|---|---|---|
| `static/psy_v3.css` | 新增 | 23,180 B |
| `static/shell_panels.js` | 更新（psy 面板第 11–124 行替换为 v3） | 29,254 → 41,496 B |
| `templates/shell.html` | 更新（新增 2 处 `<link>`） | 20,406 → 21,477 B |

`test2.py` **本次未改动**（纯静态资源与模板，无需新增路由）。
`templates/index.html` **未触碰**，md5 仍为 `a6f582c049f1a5e86662d36e2184983d`。

### 命名隔离（关键设计）
外壳与设计稿色板不同（外壳主青 `#00F7FF`，v3 稿主青 `#00D4FF`），必须防止互相污染：

- 所有选择器统一置于 `.psy-v3` 作用域下 —— 脚本校验：**0 个未隔离选择器**
- 所有自定义属性使用 `--pv3-*` 前缀 —— 15 个，与外壳 `--cyan` 等零重名
- 动画 `@keyframes pv3-pulse` 加前缀，避免与外壳动画同名

**双向渗漏验证**：
- **外壳 → 面板**：面板内确实用到 7 个短类名（`k` `d` `n` `v` `t` `lb` `cur`）与外壳重名，
  但外壳的对应规则全部形如 `.kv .k` / `.timeline .item .d` / `.nav-item .txt`，
  依赖 `.kv` `.timeline` `.nav-item` 等**面板内不存在的祖先**，选择器无法命中。
  脚本判定：**0 条可渗漏规则**。
- **面板 → 外壳**：`psy_v3.css` 全部选择器带 `.psy-v3` 前缀，无法作用到外壳。
- 外壳仅有的两条全局规则是 `*{box-sizing/margin/padding}` 与 `html,body{}`，
  前者本身也是设计稿的预期基线，无冲突。

### 字体处理（避免境内不可达导致白屏）
设计稿使用 Noto Sans SC + JetBrains Mono。服务器位于境内，Google Fonts 可能不可达，
若用普通 `<link rel="stylesheet">` 会**阻塞首屏渲染**。

采用 `media="print" onload="this.media='all'"` 技巧改为**非阻塞加载**：
取不到时自动回退到 `psy_v3.css` 声明的本地字体栈
（`PingFang SC` / `Microsoft YaHei` / `Consolas` 等），不影响任何渲染。

### 响应式断点（替代设计稿的 scale 方案）
| 断点 | 降级行为 |
|---|---|
| `≤1500px` | 结论通栏三栏 → `score+concl` 上排、`dominant` 下排 |
| `≤1200px` | 左右 66:34 分栏 → 单列；Layer2/Layer3 双卡 → 单列 |
| `≤820px` | 结论通栏全单列；五维/量表行收窄；AI 卡片字号 15→14px |

### 视觉规格 100% 沿用设计稿
数据全部为设计稿原值（示例数据，弹窗顶部已有「示例数据 · 未接入实时模型」标识）。
**49 项数据保真校验全部通过**，含：

- 评分环 81/100：`stroke-dasharray="263.9"` `stroke-dashoffset="50.1"`，渐变 `#00E5CC→#00D4FF`
- 五维画像 86 / 88 / 79 / 72 / 33（压力为反向指标，amber 色 + 「越低越好」标签）
- 情绪构成 平静72 / 愉快15 / 悲伤6 / 惊讶4 / 愤怒2 / 恐惧1
- 量表估算 PSS 26低 / GAD-7 18低 / PHQ-9 12低 / 疲劳 41中(amber)
- 情绪时间线 3 条 SVG 路径（`#00D4FF` / `#FFB020` / `#c8535f`）+ 网格线原样
- AI 解读文案**一字未改**（README 明确要求）
- 语音波形 64 根柱，`progress=0.7`，高度公式 `8+|sin(i*0.45)*14+cos(i*0.23)*8|`

### 双轨设计保留（合规要点）
设计稿用**左右分栏**区分数据来源，本次完整保留：
- 左栏 `PASSIVE · 被动式感知`（摄像头无感采集）—— 量表卡片标注 chip「生理 · 行为估算」，
  并保留免责声明「非临床诊断依据」
- 右栏 `ACTIVE · 主动式交互`（需用户参与）—— 提供 PSS-10 / GAD-7 / PHQ-9 / PSQI 真实问卷入口

这一设计解决了此前提出的合规隐忧：**由视频信号推断临床量表分值**不宜直接等同于量表结果，
设计稿通过「估算值」标注 + 真实自评问卷交叉校准的双轨方式规避了该风险。

### 校验与部署
```bash
bash deploy_psy_v3.sh --dry-run   # 只看差异
bash deploy_psy_v3.sh             # 部署
bash deploy_psy_v3.sh --rollback  # 回滚
bash start_gunicorn_8801.sh       # 重启生效
```

脚本内置 5 道前置闸门，任一不过即拒绝写入：
1. 源文件齐备
2. 首页 md5 == `a6f582c049f1a5e86662d36e2184983d`
3. `shell_panels.js` 括号/模板字符串平衡 + 5 个面板齐全 + psy 含 `.psy-v3` 根节点
4. `shell.html` 已引用 `psy_v3.css` 且 iframe 指向 `/max_inner`（存在 `src="/max"` 即拒绝，防外壳递归自加载）
5. `psy_v3.css` 全部选择器隔离在 `.psy-v3` 下

写入后再次校验首页 md5 与逐字节一致性，任一失败自动回滚。

备份：
- `static/shell_panels.js.bak_psyv3_2026-08-12_171602`
- `templates/shell.html.bak_psyv3_2026-08-12_171602`

### 验证结果

| 路由 | 状态 | 大小 |
|---|---|---|
| `/max` | 200 | 21392（外壳页，含 v3 引用）|
| `/max_inner` | 200 | 108193（原首页）|
| `/v2` | 200 | 21392 |
| `/` | 200 | 21052 |
| `/jc` | 200 | 20954 |
| `/static/psy_v3.css` | 200 | 23180 |
| `/static/shell_panels.js` | 200 | 41496 |

- **首页零改动**：`/max_inner` 渲染 md5 = `5d6e763d324e337a94e57f287f7f5b4e`，与历次一致
- **防递归**：`/max` 的 iframe 指向 `/max_inner`；`/max` 中 `src="/max"` 出现 **0** 次；`/max_inner` 内 iframe 数 **0**
- **模板变量正确解析**：`{{ url_for(...) }}` → `/static/psy_v3.css`
- **加载顺序正确**：`psy_v3.css` 位于外壳 `</style>` 之后（第 299 行），级联优先级符合预期
- **DOM 结构完整**：标签闭合零错误；5 个五维行 / 6 个情绪行 / 4 个量表行 / 4 个自评项 / 64 根波形柱 / 4 个 SVG
- **类名覆盖 1:1**：markup 使用 70 个 `pv3-` 类，CSS 定义 70 个，**无缺失、无冗余**

> 注：本次未能用浏览器截图验证（Playwright 工具报 `Target page/context closed`，
> 且沙箱内无本地浏览器与 node 运行时）。改用等价的静态验证替代：
> 用 Python 复算模板字符串插值 → 得到真实 HTML → 校验标签闭合、类名覆盖、
> 49 项数据保真、选择器可达性分析。这些检查覆盖了截图能发现的结构性问题，
> 但**最终视觉效果仍建议在浏览器中人工确认一次**。

### 本次范围之外（已与用户确认）
- **语音录音**：本轮仅做视觉还原（静态波形 + 麦克风按钮 `title="录音功能开发中"`）。
  真实 `getUserMedia` / Web Audio 采集单独立项。
- **真实数据接入**：本轮沿用设计稿示例值。后端已具备的可用字段：
  `psycho{relax,hesitate,nervous}`、`emo_distribution`、`emo_stability`、`emo_switches`、
  `attention`、`blink_rate`、`gaze_stability`、`fatigue`、`perclos`、`au_symmetry`、`au_activity`，
  以及 `/get_hr` 的 `heart_rate` / `respiration_rate`。
- **待确认参数**：压力/活力维度权重、心率基线校准方式、睡眠指数数据来源。

---

## 2026-08-12 17:3x · 布局优化：解决内容溢出与「卡片被砍头」

### 问题定位
用户三张截图暴露同一个根因，不是样式错乱而是**高度预算超支**：

1. 首屏底部「AI 综合解读」「量表自评」被裁掉，看不全
2. 往下滚动后，顶部卡片滑到 `.p-head` 之下被裁切，出现「卡片被砍头」的观感
3. 滚动条本身也说明内容装不下

按 CSS 固定尺寸逐项累加，量化了超支幅度：

| 项 | 高度 |
|---|---|
| 结论通栏 | 110 |
| split（取较高列，瓶颈是**右栏** 744）| 744 |
| 底部信息条 | 16 |
| `.p-body` 内边距 (22+28) | 50 |
| `.p-head` + `.p-foot` (58+28) | 86 |
| 外壳顶栏 `--top-h` | 70 |
| **需要视口** | **1092 px** |

用户视口约 940px（1080p 减去浏览器 chrome + 多标签栏），**超支约 152px**，正好等于被裁掉的那一块。

### 修复思路
与其让它滚动，不如让它**装得下**。压缩的全部是「非信息性空间」——
内边距、行距、装饰字号、图表留白，**不改任何数据、不删任何字段**。

**基准档收紧（37 处）**：
- `.p-body` 内边距 22/28/28 → 12/16/14（用 `:has(> .psy-v3)` 只作用于本面板）
- 卡片内边距 12/16 → 10/14；卡片标题下边距 10 → 8
- 主 flex 间隙 8 → 6；分栏 gap 12 → 10；Layer gap 10 → 8
- 评分环 82 → 70px；数字 32 → 27px
- 五维行 22 → 20px；情绪条 18 → 16px；量表行距 10 → 7px
- 情绪时间线 160 → 126px
- 语音波形 44 → 34px；麦克风 56 → 46px
- AI 正文 15px/1.9 → 14px/1.75，内边距 20/24 → 13/18

**新增 3 档「视口高度」断点**（这是本次核心，此前只有宽度断点）：

| 断点 | 应对场景 | 降级 |
|---|---|---|
| `max-height:940px` | 1080p 全屏 | 图表 112px、AI 13.5px |
| `max-height:880px` | 1600×900 / 带书签栏的 1080p | 全面收紧，图表 96px、评分环 62px |
| `max-height:800px` | 1366×768 小笔记本 | 图表 84px、隐藏量表副标题与公式脚注 |

`max-height:800px` 档隐藏的两项是**冗余信息**，不是数据：
- 量表副标题「10 题 · 约 2 min」→ 量表名本身已足够识别
- 五维图例的公式脚注 → 该公式已存在于每行 ⓘ 的 tooltip 中

### 效果
需要视口 **1092px → 912px**（降 180px）：

| 视口 | 修复前 | 修复后 |
|---|---|---|
| 1080 | 溢出 12 | **余量 168** |
| 1000 | 溢出 92 | **余量 88** |
| 940（用户实际）| **溢出 152** | **余量 28** |
| 900 | 溢出 192 | 溢出 12（880 档接管）|
| 800 | 溢出 292 | 800 档接管 |

用户视口从「溢出 152px」变为「余量 28px」，**不再滚动**，砍头现象随之消失。

### 部署脚本同步更新
- 闸门 5 放行唯一例外 `.p-body:has(> .psy-v3)`（仍受 `.psy-v3` 限定，
  只命中承载本面板的那个 `.p-body`，不影响其余 4 个面板）
- **新增闸门 6**：校验 3 档高度断点存在，且图表高度逐档递减
  （126→112→96→84），防止后续改动把高度预算改回去

### 验证
- 6 道闸门全绿；`--dry-run` 确认**仅 `psy_v3.css` 变更**，
  `shell_panels.js` / `shell.html` 均报「一致」
- 首页 md5 `a6f582c0..` 未变；`/max_inner` 渲染 md5 `5d6e763d..` 未变
- 线上 CSS 26,055 B，含 3 条 `max-height` 规则、4 档图表高度、2 条 `:has()` 规则
- 数据零改动：评分 81、五维 86/33、情绪 72、量表 41、AI 文案、
  时间线路径、64 柱波形、免责声明 —— 全部抽检 OK
- 选择器隔离仍为 0 泄漏；类名覆盖仍 1:1

> `:has()` 的兼容性兜底：Chrome 105+ / Safari 15.4+ / Firefox 121+ 支持。
> 旧浏览器不支持时该规则整条失效，退回外壳原内边距，只是稍不紧凑，
> **不会错版**——因为高度断点里的其余压缩项照常生效。

---

## 2026-08-12 17:53 · 移除弹窗标题带，主体内容上移

**需求**：截图标红的 58px 横向标题带（`.p-head`）去掉，主体内容上移。

**移除前的依赖排查**（关键）：标题带里住着两个不能一起删的东西——
1. `[data-close]` 关闭按钮：JS 用 `querySelector('[data-close]')` 绑定事件，
   删掉会返回 null 抛错，弹窗再也关不掉（ESC 与遮罩虽仍可用，但鼠标入口全失）；
2. 「示例数据 · 未接入实时模型」合规声明：丢了会让示例数字看着像真实测量结果。

两者一并迁至 `.p-foot`。曾考虑把关闭按钮浮在右上角，但那个位置压住心理面板的
「主导情绪」卡与生理面板的「核心生理指标」，在无法截图验证的前提下不冒这个险。

**改动**：
| 项 | 前 | 后 |
|---|---|---|
| `.p-head` | 58px 色带 | **删除** |
| `.p-body` 内边距 | 22/28/28 | 14/28/20 |
| `.p-foot` | height 28px | flex 0 0 32px（容纳迁入项） |
| 工具按钮 | 30px | 22px |
| 面板标题 | `.p-head h2` 18px | `.p-foot .breadcrumb`（身份标识）|

**弹窗外壳纵向开销 182px → 128px**（顶栏70 + 内边距34 + 底栏32），
所需视口 912 → **858px**。原 3 档 `max-height` 断点保留为安全垫。

**底栏新增 3 级宽度降级**（多承载 3 个文字项后防挤压）：
1500px 隐藏 ESC 提示 → 1100px 隐藏版本号 → 900px 隐藏运行状态。
**关闭按钮与合规声明永不隐藏。**

**新增 Gate 7**：断言 `.p-head` 不再渲染，且 `data-close`/`data-export`/
`mock-note`/`breadcrumb` 全部存在于 `.p-foot` 内、JS 仍绑定 `[data-close]`。
删标题带时顺手带走关闭按钮是此类改动最典型的翻车方式，用闸门钉死。

**验证**：7 道 Gate 全绿；`shell_panels.js` **零改动**（5 个面板内容未受影响）；
首页 md5 `a6f582c0..`、`/max_inner` 渲染 md5 `5d6e763d..` 均未变；
线上 `class="p-head"` 出现 0 次，`data-close` 2 次，递归 0 次。
